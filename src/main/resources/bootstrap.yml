spring:
  application:
    name: eureka-naming-server
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
#  contextPath: /eureka-naming-server

logging:
  level:
    org:
      springframework: info
